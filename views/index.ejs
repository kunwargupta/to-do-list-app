<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>To Do App</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>

    
    <div id="main" class="w-full min-h-screen bg-zinc-900">
        <div id="form" class="px-10 py-10">
            <form action="/create" method="post">
                <input name="title" type="text" placeholder="Title"  class="block w-full rounded-md outline-none text-white  px-5 py-2 bg-zinc-800">
                <textarea name="task" name="" id="" placeholder="Write your task here..." class="block px-5 py-2 bg-zinc-800 resize-none mt-2 w-full rounded-md outline-none text-white"></textarea>
                <button class="mt-2 text-white bg-blue-600 p-1 rounded-full px-2">Create Task</button>
            </form>
        </div>
        <div id="tasks" class="px-10 py-10 flex gap-3 flex-col flex-wrap">

            <% if(files.length > 0)  {%>
                <% files.forEach(function(file) { %>
                    <% let idx = file.indexOf('.'); %>
                    <% let fileName = file.slice(0, idx); %>
                    <div id="task"  class="bg-zinc-800 rounded-md p-2 w-full flex justify-between px-4 items-center">
                       <div id="left">
                        <h1 class="font-2xl text-white text-3xl text-start tracking-tighter"><%= fileName %></h1>
                        <a class="text-blue-500" href="/tasks/<%= file %>">read more</a> 
                        <p class="text-white hidden">task</p>
                       </div>
                        
                        <form action="/delete" method="post" id="right">
                            <!-- Hidden input to pass the file name -->
                            <input type="hidden" name="fileName" value="<%= file %>">
                            <button type="submit" id="dlt_btn"  class="mt-2 text-white bg-red-600 p-1 rounded-full px-2">Delete</button>
                        </form>
                        
                    </div>
                    <% })%>
                    <% } else {%>
                        <h3 class="text-white text-6xl text-center ">No Tasks yet!</h3>
                        <% } %>


        </div>

        
    </div>
</body>
</html>