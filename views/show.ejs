<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= fileName %></title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body>
    <div id="main" class="w-full min-h-screen bg-zinc-900 p-10">
        <% let idx = fileName.indexOf('.')  %>
        <% let title = fileName.substring(0, idx)  %>
        <h1 class="text-white text-3xl font-md">Task: <%= title %></h1>
        <div id="task_details  "  class="p-10 mt-4 bg-zinc-700 h-[600px] w-full rounded-md">
            <p class="text-white"><%= fileContent %></p>
        </div>
    </div>
</body>
</html>